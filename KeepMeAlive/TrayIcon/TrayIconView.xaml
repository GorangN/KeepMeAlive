<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:tb="http://www.hardcodet.net/taskbar">

    <tb:TaskbarIcon x:Key="TrayIcon"
                    IconSource="/Resources/Icons/tray_inactive.ico"
                    ToolTipText="KeepMeAlive"
                    LeftClickCommand="{Binding ShowWindowCommand}"
                    DoubleClickCommand="{Binding ShowWindowCommand}">
        <tb:TaskbarIcon.ContextMenu>
            <ContextMenu Style="{DynamicResource MahApps.Styles.ContextMenu}"
                         Background="{DynamicResource SurfaceContainerLowestBrush}"
                         Foreground="{DynamicResource OnSurfaceBrush}"
                         BorderBrush="{DynamicResource OutlineVariantBrush}"
                         BorderThickness="1"
                         HasDropShadow="True">
                <ContextMenu.Resources>
                    <Style TargetType="MenuItem"
                           BasedOn="{StaticResource MahApps.Styles.MenuItem}">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="{DynamicResource OnSurfaceBrush}"/>
                        <Setter Property="FontFamily" Value="{DynamicResource InterFont}"/>
                        <Setter Property="FontSize" Value="13"/>
                        <Setter Property="FontWeight" Value="Medium"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="MinWidth" Value="210"/>
                        <Setter Property="Padding" Value="12 8"/>
                    </Style>

                    <Style TargetType="Separator">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Separator">
                                    <Border Height="1"
                                            Background="{DynamicResource OutlineVariantBrush}"
                                            Opacity="0.35"
                                            Margin="12 4"/>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ContextMenu.Resources>

                <MenuItem Header="Start" Command="{Binding TrayStartCommand}"/>
                <MenuItem Header="Stop" Command="{Binding TrayStopCommand}"/>
                <Separator/>
                <MenuItem Header="Open KeepMeAlive" Command="{Binding ShowWindowCommand}"/>
                <Separator/>
                <MenuItem Header="Exit" Command="{Binding ExitApplicationCommand}"/>
            </ContextMenu>
        </tb:TaskbarIcon.ContextMenu>
    </tb:TaskbarIcon>

</ResourceDictionary>
